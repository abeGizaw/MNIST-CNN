import subprocess

for i in range(10):
    # subprocess.run(["python", "TrainingFeedForward.py"])
    subprocess.run(["python", "TrainingConvolve.py"])
    # subprocess.run(["python", "TrainingFull.py"])